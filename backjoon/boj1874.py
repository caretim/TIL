import sys

n= int(input())


arr= []


nums =[]

result=[]


def che(x):
    if arr[0]< a:
        if arr[0]==nums.pop():
            result.append('-')


for ne in range(n):
    arr.append(int,sys.stdin.readline().strip())


for num in range(n):
    if num == arr[0]: # 숫자와 어레이의 첫숫자가 같다면 
        a=arr.pop(0)    # 어레이 에서 첫숫자를 팝 . #그리고 nums리스트에 추가를 안해준다.
        result.append('-') # result 리스트에 - 추가 
        if arr[0]< a:
            if len(nums) >= 1 and arr[0]==nums:
    else:
        nums.append(num)
        result.append('+') # 어레와 숫자가 같지 않다면 넘즈 +1 
